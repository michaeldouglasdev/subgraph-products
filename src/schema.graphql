extend schema @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key"])

type Query {
  products: [Product]!
}

interface MutationResponse {
  code: Int!
  message: String!
  success: Boolean!
}

type Product {
  sku: String!
  name: String!
}
